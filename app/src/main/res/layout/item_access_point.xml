<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="14dp"
    android:paddingHorizontal="16dp"
    android:background="?android:attr/selectableItemBackground">

    <!-- Signal icon -->
    <ImageView
        android:id="@+id/iv_signal"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/ic_signal_3"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Connected indicator icon (overlaid on signal icon bottom-right, visible only for pinned AP) -->
    <ImageView
        android:id="@+id/iv_connected"
        android:layout_width="12dp"
        android:layout_height="12dp"
        android:src="@drawable/ic_link"
        android:tint="#4CAF50"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="@id/iv_signal"
        app:layout_constraintBottom_toBottomOf="@id/iv_signal" />

    <!-- SSID name -->
    <TextView
        android:id="@+id/tv_ssid"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:text="WiFi Network"
        android:textColor="@color/black"
        android:textSize="17sp"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintStart_toEndOf="@id/iv_signal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/tv_rssi" />

    <!-- RSSI signal strength -->
    <TextView
        android:id="@+id/tv_rssi"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="12dp"
        android:text="-65 dBm"
        android:textColor="#888888"
        android:textSize="13sp"
        app:layout_constraintEnd_toStartOf="@id/tv_score"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Score display (using pale blue text) -->
    <TextView
        android:id="@+id/tv_score"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:text="--"
        android:textColor="@color/pale_blue"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@id/iv_expand"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Expand button (default down icon) -->
    <ImageView
        android:id="@+id/iv_expand"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_expand_more"
        android:contentDescription="Expand"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Expanded content: Reason -->
    <TextView
        android:id="@+id/tv_reason"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:padding="12dp"
        android:background="#F8F9FA"
        android:text="Reason text goes here..."
        android:textColor="#444444"
        android:textSize="14sp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/tv_ssid"
        app:layout_constraintStart_toStartOf="@id/tv_ssid"
        app:layout_constraintEnd_toEndOf="parent"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>
